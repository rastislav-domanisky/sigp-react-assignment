import React, { ReactElement } from 'react';
import './style.scss';

function Footer(): ReactElement {
  return <footer className="footer">SIGP, s.r.o. React.js assignment</footer>;
}

export default Footer;
